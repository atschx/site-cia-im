// 注意：此文件将在未来版本中被弃用
// 请直接从 './pagination/usePagination' 或 './index' 导入
// 例如: import { usePagination } from '../hooks';

import usePagination from './pagination/usePagination';
export { usePagination };
export default usePagination; 